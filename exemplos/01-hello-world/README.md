# Exemplo 01 — Hello World

Execute:

```bash
dotnet run --project exemplos/01-hello-world/MinimalApiHello.csproj
```

Objetivo: enxergar apenas o ciclo `builder → app → endpoint → Run`, sem banco, JWT ou arquitetura em camadas.
